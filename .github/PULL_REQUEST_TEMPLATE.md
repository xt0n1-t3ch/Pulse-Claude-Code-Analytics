<!-- Thanks for contributing to Pulse! -->

## Summary

<!-- What does this PR do and why? -->

## Contract and compatibility

- Provider(s):
- Tauri command/event/schema impact:
- Config / database migration impact:
- Canonical core version and immutable SHA:
- Platform impact (Windows/macOS/Linux):

## Type of change

- [ ] 🐛 Bug fix
- [ ] ✨ Feature
- [ ] 📝 Docs
- [ ] ♻️ Refactor
- [ ] ⚡ Performance
- [ ] 🧪 Tests
- [ ] 🔧 Tooling / CI

## Checklist

- [ ] `cargo fmt --all` is clean
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [ ] `cargo test --workspace` passes
- [ ] `npm --prefix frontend run check` passes without warnings
- [ ] `npm --prefix frontend run test` passes
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Config/database migrations have fixture coverage and rollback behavior
- [ ] Canonical core dependency and manifest resolve to the same immutable SHA
- [ ] Docs updated (if user-facing change)
- [ ] `CHANGELOG.md` updated (if user-facing change)

## Runtime proof

<!-- Attach redacted native evidence. UI changes require Dark and Light at 1280×860, 900×600, and 720×560. Rich Presence changes require preview and real Discord proof. -->

## Performance proof

<!-- Record affected before/after values: startup, idle CPU/memory, Tauri invokes/events, JSONL reads, SQLite writes/query plan, and initial gzip. “Not affected” needs a reason. -->

## Screenshots / recording

<!-- Drop any UI changes here. -->

## Risk and rollback

<!-- Name the reversible boundary and exact rollback action. -->

Closes #
